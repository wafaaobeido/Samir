@using Models
@model IEnumerable<Models.Message>

@{
    ViewBag.Title = "OneConversation";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*@if (Session["User"] == null)
    {
        Response.Redirect("../Product/ViewProducts");
    }
    else
    {*@
@{

    var user = Session["User"] as User;

}
@if (user != null)
{
    <h2>OneConversation</h2>

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sender)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subject)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Body)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Sender)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subject)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Body)
            </td>
        </tr>
        }
    </table>


    @Html.ActionLink("Bericht sturen", "CreateMessage", "Message", new { recipientid = Model.First().Sender.Id, senderid = user.Id, productid = Model.First().Product.Id }, null)

}